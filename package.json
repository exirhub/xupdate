{
  "name": "xupdate",
  "version": "0.1.0",
  "private": true,
  "description": "Independent Nginx and 3x-ui deployment with a local static frontend",
  "scripts": {
    "build:css": "tailwindcss -i ./website/src/input.css -o ./website/assets/site.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}

